#pragma once

static bool IsKeyPressedWrapper(int key);
static bool IsKeyReleasedWrapper(int key);
static bool IsKeyDownWrapper(int key);

static bool IsMouseButtonPressedWrapper(int button);
static bool IsMouseButtonReleasedWrapper(int button);
static bool IsMouseButtonDownWrapper(int button);

static bool IsGamepadButtonPressedWrapper(int button);
static bool IsGamepadButtonReleasedWrapper(int button);
static bool IsGamepadButtonDownWrapper(int button);
